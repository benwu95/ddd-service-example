components:
  schemas:
    Status:
      description: status
      type: string
      enum:
        - created
        - voided
    OperationHistoryType:
      description: operation history type
      type: string
      enum:
        - created
        - updated
        - voided
