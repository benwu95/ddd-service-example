type: object
properties:
  type:
    $ref: enum.yml#/components/schemas/OperationHistoryType
  data:
    description: data
    type: array
    items:
      type: object
      properties:
        field:
          description: field name
          type: string
        before:
          description: old value
          nullable: true
        after:
          description: new value
          nullable: true
  doer:
    description: doer
    $ref: ../auth.yml#/components/schemas/User
  createdAt:
    description: created time (utc timestamp)
    type: number
